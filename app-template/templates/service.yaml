apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name | default (include "base-service.fullname" .) }}
  labels:
    {{- include "base-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "base-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
